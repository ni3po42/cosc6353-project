const React = require('react');
const IoCContainer = require("../components/IoC.js");

describe("IoCContainer Tests", () => {
 
  it("Should exist", () => {
  
    expect(IoCContainer).toBeDefined();
  });
});